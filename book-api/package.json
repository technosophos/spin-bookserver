{
    "name": "book-api",
    "version": "1.0.0",
    "description": "API server for recommending a book",
    "main": "index.js",
    "scripts": {
        "build": "npx webpack --mode=production && npx mkdirp target && npx j2w -i dist.js -d combined-wit -n combined -o target/book-api.wasm ",
        "test": "echo \"Error: no test specified\" && exit 1",
        "postinstall": "knitwit"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "mkdirp": "^3.0.1",
        "webpack": "^5.74.0",
        "webpack-cli": "^4.10.0"
    },
    "dependencies": {
        "@fermyon/spin-sdk": "^2.3.0"
    }
}